#ifndef RENDERCLASS_H
#define RENDERCLASS_H

class Renderclass
{
	public:
		//Renderclass(); //Den gillar in constructorerna :S
		//~Renderclass();
		
		void RenderObject(GLuint vbuffer, GLuint cbuffer);
		void brutalBuffer(GLuint vbuffer, GLuint cbuffer);
	private:

};

#endif
