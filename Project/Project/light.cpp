
/*


normal = (vertex.x + vertex.y + vertex.z);
normalized = sqrt(vertex.x*vertex.x + vertex.y*vertex.y + vertex.z+vertex.z);


Beräkna vinkel till normal för polygon
if(vinkel mellan normal och ljuskälla < 90)
{
	Färg * cos(vinkel);
}
else
{
	färg * 0.05;
}

Räkna absolutbeloppet ifrån ljuskälla till kub 



*/